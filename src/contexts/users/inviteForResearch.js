export const inviteForResearch = ({ userId, email }) => {
  // TODO IMPLEMENT

  // 1. check if user exists
  // 2. encrypt email and add to user: invitations: { research: <encrypted_mail>, status: 'pending' }
  // 3. try to send email
  // 4. update status (sent or error)
}
