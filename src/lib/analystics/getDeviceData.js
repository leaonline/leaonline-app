// import * as Cellular from 'expo-cellular';
// import Constants from 'expo-constants'
// import * as Device from 'expo-device'
// import * as FileSystem from 'expo-file-system'
// import NetInfo from '@react-native-community/netinfo';
// npx expo install expo-network
// Platform, PixelRatio
// TTS -> avaiable voices

export const getDeviceData = async () => {
  /*
  const data = {
    manifest: { ...Constants.manifest },
    webviewUserAgent: await Constants.getWebViewUserAgentAsync(),
    device: { ...Device },
    deviceType: await Device.getDeviceTypeAsync(),
    uptime: await Device.getUptimeAsync(),
    maxMemory: await Device.getMaxMemoryAsync(),
    isRoot: await Device.isRootedExperimentalAsync(),
    sideloadingEnabled: await Device.isSideLoadingEnabledAsync(),
    platformFeatures: await Device.getPlatformFeaturesAsync(),
    fileSystem: {
      capacity: await FileSystem.getTotalDiskCapacityAsync(),
      free: await FileSystem.getFreeDiskStorageAsync()
    }
  }
   */
}
